import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: { main: "#996515", light: "#A77C37", dark: "#674222" },
    secondary: { light: "#D3AF37", main: "#C59401", dark: "#A77C37" },
  },
});